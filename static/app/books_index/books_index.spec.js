'use strict';

describe('bookshelf.books_index module', function() {

  beforeEach(module('bookshelf.books_index'));

  describe('BooksIndexCtrl controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('BooksIndexCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});