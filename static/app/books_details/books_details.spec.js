'use strict';

describe('bookshelf.books_details module', function() {

  beforeEach(module('bookshelf.books_details'));

  describe('BooksDetailsCtrl controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('BooksDetailsCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});